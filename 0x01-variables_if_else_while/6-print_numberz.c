#include<stdio.h>
/**
* main - A program that prints all single digits numbers of base 10
* Return: 0 (success)
*/
int main(void)
{
int digit;
for (digit = 0; digit <= 9; digit++)
{
printf("%d", digit);
}
printf("\n");
return (0);
}
