#include "main.h"

/**
 * _strchr -  a function that locates a character in a string
 * @s: pointer
 * @c: pointer
 * Return: char
 */
char *_strchr(char *s, char c)
{
unsigned int length;
while (*s != '\0')
{
if (*s == C)
{
return (s);
}
s++;
}
return (NULL);
}
