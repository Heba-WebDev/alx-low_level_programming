#include "main.h"

/**
 * _abs(int) - prints the absolute value of the number
 * @n: parameter to be printed
 * Return: 0 (success)
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (n * -1);
}
}
