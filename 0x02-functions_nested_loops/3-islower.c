#include "main.h"

/**
 * int _islower(int c) - prints if a letter is lower cased or not
 *
 * Return: 0 (lowecase) 1 (uppercase)
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (0);
}else
{
return (1);
}
}
